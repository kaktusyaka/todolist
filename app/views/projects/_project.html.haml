%div{ id: dom_id(project) }
  %h4= best_in_place project, :name, activator: "#edit_#{dom_id(project)}"
  = link_to 'Edit', 'javascript:;', id: "edit_#{dom_id(project)}"
  = link_to 'Destroy', project, method: :delete, confirm: 'Are you sure?'
